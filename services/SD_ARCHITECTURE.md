# Архитектура подсистемы хранения данных DX7 RP2350 (Ветка: main)

## 1. Систематизация файлового каталога и периферии
* **Чип:** RP2350 (ARM Cortex-M33, частота 150 МГц, Pico SDK v2.x).
* **Шина SPI0 (Дисплей TFT ST7789):** Пины GP0 (RX), GP1 (CS), GP2 (SCK), GP3 (TX). Частота: штатная.
* **Шина SPI1 (SD-Карта MicroSD):** Пины GP8 (RX), GP9 (CS), GP10 (SCK), GP11 (TX). Частота: 400 кГц (стабильный режим без разгона).
* **Шина I2C (Тач-нумпад MPR121):** Пины согласно `hw_config.h`. Используется блокирующий опрос с таймаутом.
* **Интерфейс MIDI (UART):** Прерывания по приему и отправке SysEx-данных в реальном времени.

## 2. Текущая конфигурация памяти и FatFS (lib/fatfs/ffconf.h)
* `FF_USE_LFN = 2` — Длинные имена файлов (LFN) включены, рабочий буфер выделяется динамически на СТЕКЕ (`sizeof = 512 байт`).
* `FF_MAX_LFN = 255` — Полная поддержка длинных имен файлов Windows/macOS.
* `SD_MAX_FILES = 32` — Жесткий лимит количества элементов в массиве RAM (размер структуры `sd_storage_t` ~1.2 КБ).
* `SD_MAX_FILENAME_LEN = 32` — Максимальная длина отображаемого имени пресета (буфер строки).

## 3. Статус стабильности на Одноядерной конфигурации (Core 0)
1. **Инициализация:** Карта монтируется стабильно (применяется программный повтор при дребезге контактов "холодного" старта).
2. **Сканирование корня (`/`):** Отрабатывает штатно, выводит папки и файлы.
3. **Сканирование подпапок:** Успешно открывает директорию `/DX7 SYNTHOXICATED PAFREAK`. Внутрь внедрен `sleep_us(5)` для предотвращения заклинивания кэша секторов FatFS при высокой плотности побайтового чтения на частоте 400 кГц.
4. **Стриминг пресетов:** Функция отправки `sd_review_send_current_file` требует склейки полного пути к файлу (`g_review_current_dir + filename`), так как FatFS сбрасывает относительный путь при переключениях меню.
